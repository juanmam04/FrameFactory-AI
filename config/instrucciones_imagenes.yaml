# Instrucciones mínimas para generación de imágenes

parametros_sd:
  width: 1920
  height: 1080
  steps: 25

# Este archivo queda intencionalmente mínimo.
# El prompt final se construye en `prompt_builder.py` usando:
# - style_lock (visual_bible.yaml)
# - character_bible.yaml
# - role_library.yaml
# - scene data (location, action, mood, key_visual, camera)

