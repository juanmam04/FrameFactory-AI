# 04 — Flow workflow

After Approve Script, Flow Pack is generated (or via Generate Flow prompts).

## References

Master character / location / object prompts — copy into Google Flow first.

## Shots

Per shot: narration, entities to use, expected `NNN.png`, copy prompt.

Batches of 10 for human organization only (not auto-sent to Flow).

## Import

Drop files or folder → Import → statuses sync from filesystem (`generated` when `images/NNN.png` exists).
